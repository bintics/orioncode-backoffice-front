# Ejemplo de variables de entorno
# Copia este archivo como .env.local y ajusta los valores según necesites

# URL base de la API
# En desarrollo local: /api (usa el proxy de Vite)
# En producción: https://tu-api-produccion.com/api
VITE_API_BASE_URL=/api
