# Ejemplo de variables de entorno
# Copia este archivo como .env.local y ajusta los valores según necesites

# URL base de la API
# En desarrollo local: /api (usa el proxy de Vite)
# En producción: https://tu-api-produccion.com/api
VITE_API_BASE_URL=/api

# URL base del BFF (Backend for Frontend)
# Si no se especifica, se usará VITE_API_BASE_URL/bff
# En desarrollo local: /api/bff (usa el proxy de Vite)
# En producción: https://tu-bff-produccion.com/api/bff
# VITE_BFF_BASE_URL=/api/bff
